# 📝 Pastebin - Распределённое backend приложение для обмена текстами

<div align="center">

![Go](https://img.shields.io/badge/go-%2300ADD8.svg?style=for-the-badge&logo=go&logoColor=white)
![MySQL](https://img.shields.io/badge/mysql-4479A1.svg?style=for-the-badge&logo=mysql&logoColor=white)
![Redis](https://img.shields.io/badge/redis-%23DD0031.svg?style=for-the-badge&logo=redis&logoColor=white)
![Apache Kafka](https://img.shields.io/badge/Apache%20Kafka-000?style=for-the-badge&logo=apachekafka)
![Docker](https://img.shields.io/badge/docker-%230db7ed.svg?style=for-the-badge&logo=docker&logoColor=white)

*Высокопроизводительное, масштабируемое backend приложение для обмена текстами на основе микросервисной архитектуры*

</div>

## 🌟 Обзор

Pastebin — это распределённое backend приложение для обмена текстами, разработанный с учётом высокой масштабируемости и производительности. Система использует микросервисную архитектуру с очередью сообщений, слоями кэширования и объектным хранилищем для эффективной обработки текстовых фрагментов любого размера.

## 🏗️ Архитектура

Сервис состоит из следующих компонентов:

```
┌─────────────┐
│   Клиент    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────────┐
│            API Сервис                       │
│  (Обработка запросов, CRUD операции)        │
└────┬─────────────────────────────────┬──────┘
     │                                  │
     │                                  ▼
     │                          ┌──────────────┐
     │                          │    Kafka     │
     │                          │  (Очередь    │
     │                          │  сообщений)  │
     │                          └──────┬───────┘
     │                                 │
     ▼                                 ▼
┌────────────┐              ┌──────────────────┐
│   MySQL    │              │  Генератор       │
│(Метаданные)│              │    хэшей         │
└────────────┘              └──────────────────┘
     │
     │
┌────┴─────┬─────────────┬──────────────┐
│          │             │              │
▼          ▼             ▼              ▼
┌────────────┐   ┌──────────┐   ┌─────────────┐
│   MinIO    │   │  Redis   │   │   Redis     │
│ (Хранение  │   │(Популярные│  │  (Счётчик   │
│  текстов)  │   │  тексты) │   │ посещений)  │
└────────────┘   └──────────┘   └─────────────┘
```

### Компоненты

1. **API Сервис** - Основная точка входа, обрабатывает все HTTP-запросы для создания, получения и управления текстовыми фрагментами
2. **Сервис генерации хэшей** - Непрерывно генерирует заданное количество уникальных хэшей заданной длины в секунду и помещает их в Kafka
3. **MySQL** - Хранит метаданные о текстах (хэш, время создания, срок действия TTL и т.д.)
4. **MinIO** - Объектное хранилище для фактического содержимого текстов (blob storage)
5. **Kafka** - Брокер сообщений, используется как буфер для предгенерированных хэшей. Hash Generator постоянно наполняет очередь, а API сервис их потребляет
6. **Redis** - Двухцелевой слой кэширования:
   - Кэширует тексты, достигшие порогового значения посещений (настраивается в конфигурации)
   - Отслеживает количество посещений каждого текста

## ✨ Возможности

- 🚀 **Высокая производительность** - Микросервисная архитектура с асинхронной обработкой
- 📦 **Масштабируемое хранилище** - MinIO для неограниченной ёмкости текстов
- ⚡ **Умное кэширование** - Redis кэш для часто используемых текстов
- 📊 **Отслеживание посещений** - Счётчик посещений в реальном времени для каждой вставки
- 🔐 **Предгенерация хэшей** - Выделенный сервис постоянно генерирует пул уникальных хэшей
- ⏱️ **Гибкое время жизни** - Настраиваемый TTL для каждого текста или использование значения по умолчанию
- 🔄 **Асинхронная обработка** - Очередь сообщений на основе Kafka для разделения сервисов
- 🐳 **Контейнеризация** - Полная поддержка Docker для удобного развёртывания

## 🛠️ Технологический стек

- **Backend**: Go
- **Базы данных**: MySQL (метаданные), Redis (кэш + счётчики)
- **Хранилище**: MinIO (объектное хранилище)
- **Очередь сообщений**: Apache Kafka
- **Контейнеризация**: Docker

## 📊 Особенности системного дизайна

### Поток генерации хэшей
1. Hash Generator постоянно генерирует хэши
2. Сгенерированные хэши помещаются в Kafka топик
3. API сервис читает готовые хэши из Kafka по мере необходимости
4. При получении запроса на создание текста, API берёт готовый хэш из буфера
5. API сохраняет метаданные в MySQL и содержимое в MinIO
6. Клиенту возвращается ответ с хэшем

### Поток получения текста
1. Проверка Redis кэша на наличие хэша
2. При попадании в кэш: возврат закэшированного содержимого
3. При промахе кэша: извлечение из MySQL (метаданные) + MinIO (содержимое)
4. Увеличение счётчика посещений в Redis
5. Если счётчик достигает порогового значения (настраивается через `popularity_threshold`), текст сохраняется в Redis кэше
6. Ответ возвращается клиенту

### Стратегия кэширования
- Тексты кэшируются только после достижения порогового количества посещений
- Это предотвращает засорение кэша редко используемыми текстами
- Счётчики посещений обновляются в Redis для аналитики в реальном времени
